{
  "name": "coin-flip",
  "version": "1.0.0",
  "private": true,
  "description": "Coin flip application with pnpm workspaces",
  "scripts": {
    "dev": "pnpm --filter miniapp dev",
    "build": "pnpm --filter miniapp build",
    "start": "pnpm --filter miniapp start",
    "lint": "pnpm --filter miniapp lint",
    "deploy:local": "./scripts/deploy-to-local.sh",
    "deploy:sepolia": "./scripts/deploy-to-sepolia.sh",
    "deploy:mainnet": "./scripts/deploy-to-mainnet.sh",
    "post-deploy:sepolia": "echo '⚠️  This script is called automatically by deploy:sepolia. Run pnpm run deploy:sepolia instead.'",
    "post-deploy:mainnet": "echo '⚠️  This script is called automatically by deploy:mainnet. Run pnpm run deploy:mainnet instead.'",
    "post-deploy:local": "echo '⚠️  This script is called automatically by deploy:local. Run pnpm run deploy:local instead.'",
    "compile:abi": "node scripts/compile-and-update-abi.js",
    "validate:abi": "node scripts/validate-abi.js",
    "deploy:help": "./scripts/help.sh"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
