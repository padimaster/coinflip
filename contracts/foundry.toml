[profile.default]
src = "src"
out = "out"
libs = ["lib"]

[rpc_endpoints]
base-sepolia = "${BASE_SEPOLIA_URL}"
base = "${BASE_URL}"

[etherscan]
base-sepolia = { key = "${ETHERSCAN_API_KEY}" }
base = { key = "${ETHERSCAN_API_KEY}" }